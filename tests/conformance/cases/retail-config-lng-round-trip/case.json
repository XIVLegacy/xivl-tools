{
  "arguments": [
    "--as",
    "config-lng"
  ],
  "expect": {
    "outcome": "ok",
    "output": "expected.json"
  },
  "fixture": {
    "fixtureId": "retail-config-lng",
    "kind": "private"
  },
  "formatId": "config-lng",
  "id": "retail-config-lng-round-trip",
  "notes": "The write half: the model is encoded back and the bytes must reproduce the input exactly. That is what a write status of supported claims.",
  "operation": "validate",
  "schemaVersion": 1
}
