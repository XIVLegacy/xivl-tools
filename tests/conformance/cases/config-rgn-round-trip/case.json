{
  "arguments": [
    "--as",
    "config-rgn"
  ],
  "expect": {
    "outcome": "ok",
    "output": "expected.json"
  },
  "fixture": {
    "kind": "public",
    "path": "tests/fixtures/public/config/rgn-body.bin"
  },
  "formatId": "config-rgn",
  "id": "config-rgn-round-trip",
  "notes": "The write half: the model is encoded back and the bytes must reproduce the input exactly. That is what a write status of supported claims.",
  "operation": "validate",
  "schemaVersion": 1
}
