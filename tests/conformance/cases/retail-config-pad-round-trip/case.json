{
  "arguments": [
    "--as",
    "config-pad"
  ],
  "expect": {
    "outcome": "ok",
    "output": "expected.json"
  },
  "fixture": {
    "fixtureId": "retail-config-pad",
    "kind": "private"
  },
  "formatId": "config-pad",
  "id": "retail-config-pad-round-trip",
  "notes": "The write half: the model is encoded back and the bytes must reproduce the input exactly. That is what a write status of supported claims.",
  "operation": "validate",
  "schemaVersion": 1
}
